/**
 * sayHello
 * 
 * Say Hello to the name passed in argument
 * 
 * @param name string
 * @returns string
 */
export function sayHello(name: string): string {
  return `Hello ${name} !`;
}

export default {
  sayHello,
};
